Public Class ValueTypes

    Dim booleanVariable As Boolean = True

    Dim shortIntegral As Short = Short.MaxValue
    Dim integral As Integer = Integer.MaxValue
    Dim longIntegral As Long = Long.MaxValue

    Dim shortDecimal As Single = Single.MaxValue
    Dim normalDecimal As Double = Double.MaxValue
    Dim longDecimal As Decimal = Decimal.MaxValue

    Dim singleCharacter As Char = "A"

    Enum Days
        Friday
        Saturday
        Sunday
    End Enum

End Class
